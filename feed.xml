<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://ramchalamkr.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://ramchalamkr.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-02-21T22:48:07+00:00</updated><id>https://ramchalamkr.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Welford’s Algorithm: The Hidden Numerical Trick in LayerNorm</title><link href="https://ramchalamkr.github.io/blog/2026/welford-algorithm-layernorm/" rel="alternate" type="text/html" title="Welford’s Algorithm: The Hidden Numerical Trick in LayerNorm"/><published>2026-02-16T05:01:00+00:00</published><updated>2026-02-16T05:01:00+00:00</updated><id>https://ramchalamkr.github.io/blog/2026/welford-algorithm-layernorm</id><content type="html" xml:base="https://ramchalamkr.github.io/blog/2026/welford-algorithm-layernorm/"><![CDATA[<p>When computing layernorm, we need to compute the mean and variance. Seems straightforward enough right? Turns out, <strong>the naive approach can fail</strong> in floating-point arithmetic, returning <strong>wrong variance</strong>!</p> <p>In this post, we’ll explore:</p> <ol> <li><strong>The numerical instability problem</strong> with naive variance computation</li> <li><strong>Three approaches</strong> to computing variance and when each fails</li> <li><strong>Welford’s algorithm</strong> - a one-pass, numerically stable solution</li> <li><strong>Pros, cons, and when to use each approach</strong></li> </ol> <hr/> <h2 id="the-problem-when-variance-computation-breaks">The Problem: When Variance Computation Breaks</h2> <p>LayerNorm normalizes each sample’s features using:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">y</span> <span class="o">=</span> <span class="p">(</span><span class="n">x</span> <span class="o">-</span> <span class="n">mean</span><span class="p">)</span> <span class="o">/</span> <span class="nf">sqrt</span><span class="p">(</span><span class="n">variance</span> <span class="o">+</span> <span class="n">eps</span><span class="p">)</span>
</code></pre></div></div> <p>Simple enough, right? But how do you compute <code class="language-plaintext highlighter-rouge">mean</code> and <code class="language-plaintext highlighter-rouge">variance</code> without losing precision?</p> <h3 id="a-concrete-failure-example">A Concrete Failure Example</h3> <p>Let’s consider this example:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Data: mean ≈ 1e8, variance ≈ 1.0
</span><span class="n">data</span> <span class="o">=</span> <span class="p">[</span><span class="mf">100000000.0</span><span class="p">,</span> <span class="mf">100000000.1</span><span class="p">,</span> <span class="mf">100000000.2</span><span class="p">,</span> <span class="p">...]</span>

<span class="c1"># Expected variance: ~1.0
# Naive computation: -28.0  ← WRONG Variance!
</span></code></pre></div></div> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">random</span>
<span class="n">random</span><span class="p">.</span><span class="nf">seed</span><span class="p">(</span><span class="mi">100</span><span class="p">)</span>

<span class="n">large_mean</span> <span class="o">=</span> <span class="mf">1e8</span>
<span class="n">true_std</span> <span class="o">=</span> <span class="mf">1.0</span>
<span class="n">data</span> <span class="o">=</span> <span class="p">[</span><span class="n">large_mean</span> <span class="o">+</span> <span class="n">random</span><span class="p">.</span><span class="nf">gauss</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">true_std</span><span class="p">)</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">1000</span><span class="p">)]</span>

<span class="c1"># Naive formula: E[X²] - (E[X])²
</span><span class="k">def</span> <span class="nf">naive_variance</span><span class="p">(</span><span class="n">data</span><span class="p">):</span>
    <span class="n">n</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">data</span><span class="p">)</span>
    <span class="n">mean</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">(</span><span class="n">data</span><span class="p">)</span> <span class="o">/</span> <span class="n">n</span>
    <span class="n">mean_sq</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">(</span><span class="n">x</span> <span class="o">*</span> <span class="n">x</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">data</span><span class="p">)</span> <span class="o">/</span> <span class="n">n</span>
    <span class="k">return</span> <span class="n">mean_sq</span> <span class="o">-</span> <span class="n">mean</span><span class="o">**</span><span class="mi">2</span>

<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Variance: </span><span class="si">{</span><span class="nf">naive_variance</span><span class="p">(</span><span class="n">data</span><span class="p">)</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="c1"># Output: -28.0  ← failure!
</span></code></pre></div></div> <p><strong>Why?</strong> We’re subtracting two huge, almost identical numbers:</p> <ul> <li><code class="language-plaintext highlighter-rouge">E[X²]</code> ≈ <code class="language-plaintext highlighter-rouge">9999999996346138.0</code></li> <li><code class="language-plaintext highlighter-rouge">(E[X])²</code> ≈ <code class="language-plaintext highlighter-rouge">9999999996346166.0</code></li> <li>Difference = only a few significant digits remain</li> <li>Float precision loss → wrong answer!</li> </ul> <hr/> <h2 id="multiple-approaches-to-computing-variance">Multiple Approaches to Computing Variance</h2> <h3 id="approach-1-computational-formula-one-pass-unstable">Approach 1: Computational Formula (One-Pass, Unstable)</h3> <p><strong>Formula</strong>: <code class="language-plaintext highlighter-rouge">Variance = E[X²] - (E[X])²</code></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">computational_formula_variance</span><span class="p">(</span><span class="n">data</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">One-pass (optimal) but numerically UNSTABLE</span><span class="sh">"""</span>
    <span class="n">n</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">data</span><span class="p">)</span>
    <span class="n">sum_x</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">(</span><span class="n">data</span><span class="p">)</span>
    <span class="n">sum_x2</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">(</span><span class="n">x</span> <span class="o">*</span> <span class="n">x</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">data</span><span class="p">)</span>
    <span class="n">mean</span> <span class="o">=</span> <span class="n">sum_x</span> <span class="o">/</span> <span class="n">n</span>
    <span class="n">mean_sq</span> <span class="o">=</span> <span class="n">sum_x2</span> <span class="o">/</span> <span class="n">n</span>
    <span class="k">return</span> <span class="n">mean_sq</span> <span class="o">-</span> <span class="n">mean</span> <span class="o">*</span> <span class="n">mean</span>  <span class="c1"># Problem
</span></code></pre></div></div> <p><strong>Pros:</strong></p> <ul> <li>✅ Single pass over data</li> <li>✅ Minimal memory</li> </ul> <p><strong>Cons:</strong></p> <ul> <li>❌ <strong>Numerically unstable</strong> when <code class="language-plaintext highlighter-rouge">variance &lt;&lt; mean²</code></li> <li>❌ Fails with FP16/FP8</li> </ul> <p><strong>When to use:</strong> Never in production!</p> <hr/> <h3 id="approach-2-definitional-formula-two-pass-stable">Approach 2: Definitional Formula (Two-Pass, Stable)</h3> <p><strong>Formula</strong>: <code class="language-plaintext highlighter-rouge">Variance = Σ(x - μ)² / n</code></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">standard_def_formula_variance</span><span class="p">(</span><span class="n">data</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Two-pass but numerically STABLE</span><span class="sh">"""</span>
    <span class="n">n</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">data</span><span class="p">)</span>
    <span class="c1"># Pass 1: Compute mean
</span>    <span class="n">mean</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">(</span><span class="n">data</span><span class="p">)</span> <span class="o">/</span> <span class="n">n</span>
    <span class="c1"># Pass 2: Compute variance
</span>    <span class="n">var</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">((</span><span class="n">x</span> <span class="o">-</span> <span class="n">mean</span><span class="p">)</span><span class="o">**</span><span class="mi">2</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">data</span><span class="p">)</span> <span class="o">/</span> <span class="n">n</span>
    <span class="k">return</span> <span class="n">var</span>
</code></pre></div></div> <p><strong>Why it’s stable:</strong> You compute deviations <code class="language-plaintext highlighter-rouge">(x - mean)</code> first, which are small numbers even when <code class="language-plaintext highlighter-rouge">x</code> is large.</p> <p><strong>Example:</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">data</span> <span class="o">=</span> <span class="p">[</span><span class="mf">1e8</span><span class="p">,</span> <span class="mf">1e8</span> <span class="o">+</span> <span class="mf">0.1</span><span class="p">,</span> <span class="mf">1e8</span> <span class="o">+</span> <span class="mf">0.2</span><span class="p">]</span>
<span class="n">deviations</span> <span class="o">=</span> <span class="p">[</span><span class="o">-</span><span class="mf">0.1</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="o">+</span><span class="mf">0.1</span><span class="p">]</span>  <span class="c1"># Small numbers!
</span><span class="n">variance</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">([</span><span class="mf">0.01</span><span class="p">,</span> <span class="mf">0.00</span><span class="p">,</span> <span class="mf">0.01</span><span class="p">])</span> <span class="o">/</span> <span class="mi">3</span> <span class="o">=</span> <span class="mf">0.00667</span>  <span class="err">✓</span> <span class="n">Looks</span> <span class="n">good</span>
</code></pre></div></div> <p><strong>Pros:</strong></p> <ul> <li>✅ <strong>Numerically stable</strong></li> <li>✅ Conceptually clear (variance definition)</li> <li>✅ Works well in FP16/FP32</li> </ul> <p><strong>Cons:</strong></p> <ul> <li>❌ Two passes over data (read data twice, unless the data can stay in memory, in which case it becomes a single pass)</li> <li>❌ Potentially more memory bandwidth usage</li> </ul> <p><strong>When to use:</strong> Default choice for simplicity and stability</p> <hr/> <h3 id="approach-3-welfords-algorithm-one-pass-stable">Approach 3: Welford’s Algorithm (One-Pass, Stable)</h3> <p><strong>Key idea:</strong> Update mean and variance incrementally without ever subtracting large numbers.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">welford_variance</span><span class="p">(</span><span class="n">data</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">One-pass AND numerically stable</span><span class="sh">"""</span>
    <span class="n">n</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">mean</span> <span class="o">=</span> <span class="mf">0.0</span>
    <span class="n">M2</span> <span class="o">=</span> <span class="mf">0.0</span>  <span class="c1"># Sum of squared deviations
</span>    
    <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">data</span><span class="p">:</span>
        <span class="n">n</span> <span class="o">+=</span> <span class="mi">1</span>              <span class="c1"># n is updated in iteration
</span>        <span class="n">delta</span> <span class="o">=</span> <span class="n">x</span> <span class="o">-</span> <span class="n">mean</span>
        <span class="n">mean</span> <span class="o">+=</span> <span class="n">delta</span> <span class="o">/</span> <span class="n">n</span>   <span class="c1"># mean is updated in every data point
</span>        <span class="n">delta2</span> <span class="o">=</span> <span class="n">x</span> <span class="o">-</span> <span class="n">mean</span>  <span class="c1"># ← Note: using updated mean
</span>        <span class="n">M2</span> <span class="o">+=</span> <span class="n">delta</span> <span class="o">*</span> <span class="n">delta2</span>
    
    <span class="k">return</span> <span class="n">M2</span> <span class="o">/</span> <span class="n">n</span>
</code></pre></div></div> <p><strong>The recurrence formula:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mean_n = mean_{n-1} + (x_n - mean_{n-1}) / n
M2_n = M2_{n-1} + (x_n - mean_{n-1}) × (x_n - mean_n)
</code></pre></div></div> <p><strong>Side note</strong>: If this looks familiar, it’s the same trick used in online softmax for Flash Attention!</p> <p><strong>Why it works:</strong></p> <ul> <li>We only work with small deviations <code class="language-plaintext highlighter-rouge">(x - mean)</code></li> </ul> <p><strong>Pros:</strong></p> <ul> <li>✅ <strong>Numerically stable</strong></li> <li>✅ <strong>Single pass</strong> over data</li> <li>✅ <strong>Parallelizable</strong> via hierarchical merge (multiple blocks can be merged in one go)</li> </ul> <p><strong>Cons:</strong></p> <ul> <li>❌ A bit more complex to implement</li> <li>❌ Requires parallel merge formula for GPU (cannot use a simple atomic instruction for the merge like split K gemm)</li> <li>❌ Additional overhead</li> </ul> <p><strong>When to use:</strong></p> <ul> <li>Production GPU kernels (FP16/BF16)</li> <li>Online statistics</li> <li>When hidden dimension &gt; single GPU max block size</li> </ul> <h2 id="critical-question-when-do-we-actually-need-welford">Critical Question: When Do We ACTUALLY Need Welford?</h2> <p><strong>You might be wondering:</strong> “If my data fits in memory, why not just use the two-pass method?” Good question! The answer depends on <em>where</em> it fits in memory—HBM vs SRAM makes all the difference.</p> <h3 id="case-1-data-fits-in-sramregisters-single-block">Case 1: Data Fits in SRAM/Registers (Single Block)</h3> <p><strong>Welford is NOT needed!</strong> ✅</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># All data already loaded into fast on-chip memory
</span><span class="n">x</span> <span class="o">=</span> <span class="nf">load_entire_row</span><span class="p">()</span>  <span class="c1"># One memory load from HBM → SRAM
</span>
<span class="c1"># Two "passes" are just loops in fast memory (SRAM/registers)
</span><span class="n">mean</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="o">/</span> <span class="n">N</span>       <span class="c1"># Loop 1: super fast
</span><span class="n">var</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">((</span><span class="n">x</span> <span class="o">-</span> <span class="n">mean</span><span class="p">)</span><span class="err">²</span><span class="p">)</span> <span class="o">/</span> <span class="n">N</span>  <span class="c1"># Loop 2: super fast
</span>
<span class="c1"># No additional HBM memory bandwidth cost!
# Definitional formula is stable
# This is what Triton does for hidden_dim ≤ 8K
</span></code></pre></div></div> <p><strong>Memory hierarchy:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>HBM (slow) → SRAM (fast) → Registers (very fast)
    ↑              ↑              ↑
One load     Two loops      Computations
</code></pre></div></div> <p><strong>Takeaway:</strong> If the entire row fits in one GPU block’s SRAM, use simple two-pass!</p> <hr/> <h3 id="case-2-multiple-blocks-parallel-processing">Case 2: Multiple Blocks, Parallel Processing</h3> <p><strong>Key point</strong>: Even in sequential processing, each block sequentially, it is still useful to use Welford - similar to flash attention approach.</p> <p><strong>Welford IS needed!</strong> ✅</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Phase 1: Each block computes local Welford IN PARALLEL (one HBM read per block)
</span><span class="n">Block</span> <span class="mi">1</span> <span class="p">(</span><span class="n">parallel</span><span class="p">):</span> <span class="n">mean_1</span><span class="p">,</span> <span class="n">M2_1</span><span class="p">,</span> <span class="n">n_1</span> <span class="o">=</span> <span class="nf">welford</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="mi">0</span><span class="p">:</span><span class="mi">4096</span><span class="p">])</span>  
<span class="n">Block</span> <span class="mi">2</span> <span class="p">(</span><span class="n">parallel</span><span class="p">):</span> <span class="n">mean_2</span><span class="p">,</span> <span class="n">M2_2</span><span class="p">,</span> <span class="n">n_2</span> <span class="o">=</span> <span class="nf">welford</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="mi">4096</span><span class="p">:</span><span class="mi">8192</span><span class="p">])</span>

<span class="c1"># Phase 2: Merge in fast memory (no HBM access!)
</span><span class="n">global_mean</span><span class="p">,</span> <span class="n">global_var</span> <span class="o">=</span> <span class="nf">welford_merge</span><span class="p">(</span>
    <span class="p">(</span><span class="n">mean_1</span><span class="p">,</span> <span class="n">M2_1</span><span class="p">,</span> <span class="n">n_1</span><span class="p">),</span> 
    <span class="p">(</span><span class="n">mean_2</span><span class="p">,</span> <span class="n">M2_2</span><span class="p">,</span> <span class="n">n_2</span><span class="p">)</span>
<span class="p">)</span>
</code></pre></div></div> <p><strong>Benefit:</strong> Only ONE HBM read per block + fast merge in SRAM!</p> <h4 id="welfords-block-merge-formula">Welford’s Block Merge Formula</h4> <p>When merging two blocks A and B:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">welford_merge</span><span class="p">(</span><span class="n">n_A</span><span class="p">,</span> <span class="n">mean_A</span><span class="p">,</span> <span class="n">M2_A</span><span class="p">,</span> <span class="n">n_B</span><span class="p">,</span> <span class="n">mean_B</span><span class="p">,</span> <span class="n">M2_B</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Combine two Welford statistics</span><span class="sh">"""</span>
    <span class="n">n_combined</span> <span class="o">=</span> <span class="n">n_A</span> <span class="o">+</span> <span class="n">n_B</span>
    <span class="n">delta</span> <span class="o">=</span> <span class="n">mean_B</span> <span class="o">-</span> <span class="n">mean_A</span>  <span class="c1"># Difference between local means
</span>    <span class="n">mean_combined</span> <span class="o">=</span> <span class="p">(</span><span class="n">n_A</span> <span class="o">*</span> <span class="n">mean_A</span> <span class="o">+</span> <span class="n">n_B</span> <span class="o">*</span> <span class="n">mean_B</span><span class="p">)</span> <span class="o">/</span> <span class="n">n_combined</span>
    <span class="n">M2_combined</span> <span class="o">=</span> <span class="n">M2_A</span> <span class="o">+</span> <span class="n">M2_B</span> <span class="o">+</span> <span class="n">delta</span><span class="o">**</span><span class="mi">2</span> <span class="o">*</span> <span class="p">(</span><span class="n">n_A</span> <span class="o">*</span> <span class="n">n_B</span><span class="p">)</span> <span class="o">/</span> <span class="n">n_combined</span>
    <span class="k">return</span> <span class="n">n_combined</span><span class="p">,</span> <span class="n">mean_combined</span><span class="p">,</span> <span class="n">M2_combined</span>
</code></pre></div></div> <p><strong>Example: Merging two blocks</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Block A: [10, 12]
</span><span class="n">n_A</span> <span class="o">=</span> <span class="mi">2</span>
<span class="n">mean_A</span> <span class="o">=</span> <span class="mf">11.0</span>
<span class="n">M2_A</span> <span class="o">=</span> <span class="mf">2.0</span>  <span class="c1"># (10-11)² + (12-11)² = 1 + 1 = 2
</span>
<span class="c1"># Block B: [14, 16]
</span><span class="n">n_B</span> <span class="o">=</span> <span class="mi">2</span>
<span class="n">mean_B</span> <span class="o">=</span> <span class="mf">15.0</span>
<span class="n">M2_B</span> <span class="o">=</span> <span class="mf">2.0</span>  <span class="c1"># (14-15)² + (16-15)² = 1 + 1 = 2
</span>
<span class="c1"># Merge:
</span><span class="n">n_combined</span> <span class="o">=</span> <span class="mi">2</span> <span class="o">+</span> <span class="mi">2</span> <span class="o">=</span> <span class="mi">4</span>
<span class="n">delta</span> <span class="o">=</span> <span class="mf">15.0</span> <span class="o">-</span> <span class="mf">11.0</span> <span class="o">=</span> <span class="mf">4.0</span>
<span class="n">mean_combined</span> <span class="o">=</span> <span class="p">(</span><span class="mi">2</span><span class="o">*</span><span class="mi">11</span> <span class="o">+</span> <span class="mi">2</span><span class="o">*</span><span class="mi">15</span><span class="p">)</span> <span class="o">/</span> <span class="mi">4</span> <span class="o">=</span> <span class="mi">52</span><span class="o">/</span><span class="mi">4</span> <span class="o">=</span> <span class="mf">13.0</span>
<span class="n">correction</span> <span class="o">=</span> <span class="mi">4</span><span class="err">²</span> <span class="o">*</span> <span class="p">(</span><span class="mi">2</span><span class="o">*</span><span class="mi">2</span><span class="p">)</span> <span class="o">/</span> <span class="mi">4</span> <span class="o">=</span> <span class="mi">16</span> <span class="o">*</span> <span class="mi">4</span> <span class="o">/</span> <span class="mi">4</span> <span class="o">=</span> <span class="mi">16</span>
<span class="n">M2_combined</span> <span class="o">=</span> <span class="mi">2</span> <span class="o">+</span> <span class="mi">2</span> <span class="o">+</span> <span class="mi">16</span> <span class="o">=</span> <span class="mi">20</span>

<span class="c1"># Verify:
# True data: [10, 12, 14, 16]
# True mean: (10+12+14+16)/4 = 13.0 ✓
# True M2: (10-13)² + (12-13)² + (14-13)² + (16-13)² = 9+1+1+9 = 20 ✓
</span></code></pre></div></div> <p><strong>Key insight:</strong> The correction term <code class="language-plaintext highlighter-rouge">delta² * n_A * n_B / n_combined</code> accounts for the fact that blocks A and B used different local means, not the global mean!</p> <hr/> <p><strong>Caveat for Layer Norm</strong></p> <p>Here’s the catch: even with Welford, you can’t avoid two HBM passes for LayerNorm:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">### Pass 1: Compute statistics
</span><span class="n">Block</span> <span class="mi">1</span><span class="p">:</span> <span class="n">local_mean_1</span><span class="p">,</span> <span class="n">local_M2_1</span> <span class="o">=</span> <span class="nf">welford</span><span class="p">(</span><span class="nf">load_chunk_1</span><span class="p">())</span>  <span class="c1"># HBM read #1
</span><span class="n">Block</span> <span class="mi">2</span><span class="p">:</span> <span class="n">local_mean_2</span><span class="p">,</span> <span class="n">local_M2_2</span> <span class="o">=</span> <span class="nf">welford</span><span class="p">(</span><span class="nf">load_chunk_2</span><span class="p">())</span>  <span class="c1"># HBM read #2
</span>
<span class="c1">### Merge (fast, in SRAM)
</span><span class="n">global_mean</span><span class="p">,</span> <span class="n">global_var</span> <span class="o">=</span> <span class="nf">merge</span><span class="p">(</span><span class="n">stats_1</span><span class="p">,</span> <span class="n">stats_2</span><span class="p">)</span>

<span class="c1">### Pass 2: Normalize (MUST re-read data from HBM!)
</span><span class="n">Block</span> <span class="mi">1</span><span class="p">:</span> <span class="n">y_1</span> <span class="o">=</span> <span class="nf">normalize</span><span class="p">(</span><span class="nf">load_chunk_1</span><span class="p">(),</span> <span class="n">global_mean</span><span class="p">,</span> <span class="n">global_var</span><span class="p">)</span>  <span class="c1"># HBM read #3
</span><span class="n">Block</span> <span class="mi">2</span><span class="p">:</span> <span class="n">y_2</span> <span class="o">=</span> <span class="nf">normalize</span><span class="p">(</span><span class="nf">load_chunk_2</span><span class="p">(),</span> <span class="n">global_mean</span><span class="p">,</span> <span class="n">global_var</span><span class="p">)</span>  <span class="c1"># HBM read #4
</span>
<span class="c1">### Total: 2 HBM reads per block + 1 HBM write per block
</span></code></pre></div></div> <p><strong>The problem</strong>: After computing statistics with Welford, the data is GONE from SRAM (limited capacity). You MUST re-read it from HBM to normalize!</p> <h3 id="summary-when-do-we-actually-need-welford">Summary: When Do We ACTUALLY Need Welford?</h3> <table> <thead> <tr> <th>Scenario</th> <th>Need Welford?</th> <th>Reason</th> </tr> </thead> <tbody> <tr> <td><strong>Single block (data in SRAM)</strong></td> <td>❌ NO</td> <td>Two-pass in fast memory is perfect</td> </tr> <tr> <td><strong>Multiple blocks, parallel</strong></td> <td>✅ YES</td> <td>Need merge formula to combine stats correctly</td> </tr> <tr> <td><strong>Streaming/online data</strong></td> <td>✅ YES</td> <td>Can’t store all data</td> </tr> </tbody> </table> <p><strong>Key insight:</strong> Modern Triton skips Welford because <strong>hidden_dim ≤ 8K fits in one block</strong>. No multiple blocks → no merge needed → simple two-pass wins!</p> <hr/> <hr/> <h2 id="production-implementations">Production Implementations</h2> <h3 id="pytorch-cuda-uses-welfords-algorithm">PyTorch CUDA: Uses Welford’s Algorithm</h3> <p><strong>Source code:</strong></p> <ul> <li><strong>File:</strong> <code class="language-plaintext highlighter-rouge">pytorch/aten/src/ATen/native/cuda/layer_norm_kernel.cu</code></li> <li><strong>Link:</strong> <a href="https://github.com/pytorch/pytorch/blob/main/aten/src/ATen/native/cuda/layer_norm_kernel.cu">PyTorch LayerNorm CUDA Kernel</a></li> </ul> <h3 id="triton-uses-two-pass-definitional-formula">Triton: Uses Two-Pass (Definitional Formula)</h3> <p><strong>Source:</strong> <a href="https://github.com/triton-lang/triton/blob/main/python/tutorials/05-layer-norm.py">Triton LayerNorm Tutorial</a></p> <p>The official Triton tutorial uses a two-pass approach — simple and stable enough when hidden_dim fits in one block.</p> <h3 id="when-welford-becomes-critical">When Welford Becomes Critical</h3> <p><strong>Use Welford when:</strong></p> <ul> <li>❗ Using FP8 or lower precision</li> <li>❗ Hidden dimension &gt; 16K (requires tiling)</li> <li>❗ Extreme data distributions (variance « mean²)</li> </ul> <p><strong>Skip Welford when:</strong></p> <ul> <li>✅ Hidden dim ≤ 8K (fits in one block)</li> <li>✅ FP32 precision</li> </ul> <hr/> <h2 id="triton-implementations">Triton Implementations</h2> <p>To make this concrete, here are two Triton kernels — one naive single-block implementation, and one tiled Welford version for larger hidden dimensions.</p> <h3 id="naive-single-block">Naive (Single-Block)</h3> <p>Assumes the entire row fits in one block’s SRAM. Two passes happen in fast memory — no extra HBM cost.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@triton.jit</span>
<span class="k">def</span> <span class="nf">layernorm_kernel</span><span class="p">(</span>
    <span class="n">x_ptr</span><span class="p">,</span> <span class="n">output_ptr</span><span class="p">,</span> <span class="n">weight_ptr</span><span class="p">,</span> <span class="n">bias_ptr</span><span class="p">,</span>
    <span class="n">M</span><span class="p">,</span> <span class="n">N</span><span class="p">,</span> <span class="n">eps</span><span class="p">,</span> <span class="n">stride_m</span><span class="p">,</span> <span class="n">stride_n</span><span class="p">,</span>
    <span class="n">BLOCK_N</span><span class="p">:</span> <span class="n">tl</span><span class="p">.</span><span class="n">constexpr</span><span class="p">,</span>
<span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Single-block LayerNorm. Assumes N &lt;= BLOCK_N (e.g., hidden_dim &lt;= 8K).
    Loads the entire row once into SRAM — mean and variance are computed from
    registers, so this is effectively a single HBM pass.
    </span><span class="sh">"""</span>
    <span class="n">pid_m</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">program_id</span><span class="p">(</span><span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
    <span class="n">offset_n</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">BLOCK_N</span><span class="p">)</span>
    <span class="n">mask</span> <span class="o">=</span> <span class="n">offset_n</span> <span class="o">&lt;</span> <span class="n">N</span>
    <span class="n">offset</span> <span class="o">=</span> <span class="n">pid_m</span> <span class="o">*</span> <span class="n">stride_m</span> <span class="o">+</span> <span class="n">offset_n</span> <span class="o">*</span> <span class="n">stride_n</span>
    <span class="n">x</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">x_ptr</span> <span class="o">+</span> <span class="n">offset</span><span class="p">,</span> <span class="n">mask</span><span class="o">=</span><span class="n">mask</span><span class="p">,</span> <span class="n">other</span><span class="o">=</span><span class="mf">0.0</span><span class="p">)</span>

    <span class="n">mean</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span> <span class="o">/</span> <span class="n">N</span>
    <span class="n">centered</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">mask</span><span class="p">,</span> <span class="n">x</span> <span class="o">-</span> <span class="n">mean</span><span class="p">,</span> <span class="n">other</span><span class="o">=</span><span class="mf">0.0</span><span class="p">)</span>
    <span class="n">var</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">sum</span><span class="p">((</span><span class="n">centered</span> <span class="o">*</span> <span class="n">centered</span><span class="p">))</span> <span class="o">/</span> <span class="n">N</span>
    <span class="n">x_norm</span> <span class="o">=</span> <span class="p">(</span><span class="n">x</span> <span class="o">-</span> <span class="n">mean</span><span class="p">)</span> <span class="o">/</span> <span class="n">tl</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">var</span> <span class="o">+</span> <span class="n">eps</span><span class="p">)</span>

    <span class="n">w</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">weight_ptr</span> <span class="o">+</span> <span class="n">offset_n</span><span class="p">,</span> <span class="n">mask</span><span class="o">=</span><span class="n">mask</span><span class="p">,</span> <span class="n">other</span><span class="o">=</span><span class="mf">0.0</span><span class="p">)</span>
    <span class="n">bias</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">bias_ptr</span> <span class="o">+</span> <span class="n">offset_n</span><span class="p">,</span> <span class="n">mask</span><span class="o">=</span><span class="n">mask</span><span class="p">,</span> <span class="n">other</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
    <span class="n">out</span> <span class="o">=</span> <span class="n">x_norm</span> <span class="o">*</span> <span class="n">w</span> <span class="o">+</span> <span class="n">bias</span>
    <span class="n">tl</span><span class="p">.</span><span class="nf">store</span><span class="p">(</span><span class="n">output_ptr</span> <span class="o">+</span> <span class="n">pid_m</span> <span class="o">*</span> <span class="n">stride_m</span> <span class="o">+</span> <span class="n">offset_n</span> <span class="o">*</span> <span class="n">stride_n</span><span class="p">,</span> <span class="n">out</span><span class="p">,</span> <span class="n">mask</span><span class="o">=</span><span class="n">mask</span><span class="p">)</span>
</code></pre></div></div> <h3 id="tiled-welford">Tiled Welford</h3> <p>For when hidden_dim &gt; BLOCK_N. Pass 1 streams through the tiles computing Welford stats and merging them. Pass 2 normalizes.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@triton.jit</span>
<span class="k">def</span> <span class="nf">layernorm_kernel_welford</span><span class="p">(</span>
    <span class="n">x_ptr</span><span class="p">,</span> <span class="n">output_ptr</span><span class="p">,</span> <span class="n">weight_ptr</span><span class="p">,</span> <span class="n">bias_ptr</span><span class="p">,</span>
    <span class="n">M</span><span class="p">,</span> <span class="n">N</span><span class="p">,</span> <span class="n">eps</span><span class="p">,</span> <span class="n">stride_m</span><span class="p">,</span> <span class="n">stride_n</span><span class="p">,</span>
    <span class="n">BLOCK_N</span><span class="p">:</span> <span class="n">tl</span><span class="p">.</span><span class="n">constexpr</span><span class="p">,</span>
<span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Tiled LayerNorm with Welford</span><span class="sh">'</span><span class="s">s algorithm. Works for any N.
    Pass 1: compute global mean/variance via Welford merge across tiles.
    Pass 2: normalize using global stats (requires re-reading from HBM).
    </span><span class="sh">"""</span>
    <span class="n">pid_m</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">program_id</span><span class="p">(</span><span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
    <span class="n">n_total</span> <span class="o">=</span> <span class="mf">0.0</span>
    <span class="n">mean_combined</span> <span class="o">=</span> <span class="mf">0.0</span>
    <span class="n">M2_combined</span> <span class="o">=</span> <span class="mf">0.0</span>

    <span class="n">num_blocks</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">cdiv</span><span class="p">(</span><span class="n">N</span><span class="p">,</span> <span class="n">BLOCK_N</span><span class="p">)</span>
    <span class="n">offset_n</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">BLOCK_N</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">b</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_blocks</span><span class="p">):</span>
        <span class="n">offset</span> <span class="o">=</span> <span class="n">pid_m</span> <span class="o">*</span> <span class="n">stride_m</span> <span class="o">+</span> <span class="p">(</span><span class="n">b</span> <span class="o">*</span> <span class="n">BLOCK_N</span> <span class="o">+</span> <span class="n">offset_n</span><span class="p">)</span> <span class="o">*</span> <span class="n">stride_n</span>
        <span class="n">col_start</span> <span class="o">=</span> <span class="n">b</span> <span class="o">*</span> <span class="n">BLOCK_N</span>
        <span class="n">mask</span> <span class="o">=</span> <span class="n">col_start</span> <span class="o">+</span> <span class="n">offset_n</span> <span class="o">&lt;</span> <span class="n">N</span>
        <span class="n">x_tile</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">x_ptr</span> <span class="o">+</span> <span class="n">offset</span><span class="p">,</span> <span class="n">mask</span><span class="o">=</span><span class="n">mask</span><span class="p">,</span> <span class="n">other</span><span class="o">=</span><span class="mf">0.0</span><span class="p">)</span>

        <span class="n">n_block</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">tl</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">mask</span><span class="p">,</span> <span class="mf">1.0</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">),</span> <span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
        <span class="n">mean_block</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">tl</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">mask</span><span class="p">,</span> <span class="n">x_tile</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">),</span> <span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span> <span class="o">/</span> <span class="n">tl</span><span class="p">.</span><span class="nf">maximum</span><span class="p">(</span><span class="n">n_block</span><span class="p">,</span> <span class="mf">1.0</span><span class="p">)</span>
        <span class="n">centered</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">mask</span><span class="p">,</span> <span class="n">x_tile</span> <span class="o">-</span> <span class="n">mean_block</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">)</span>
        <span class="n">M2_block</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">centered</span> <span class="o">*</span> <span class="n">centered</span><span class="p">,</span> <span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>

        <span class="c1"># Welford merge: combines local block stats into running global stats
</span>        <span class="n">delta</span> <span class="o">=</span> <span class="n">mean_block</span> <span class="o">-</span> <span class="n">mean_combined</span>
        <span class="n">n_new</span> <span class="o">=</span> <span class="n">n_total</span> <span class="o">+</span> <span class="n">n_block</span>
        <span class="n">n_new_safe</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">maximum</span><span class="p">(</span><span class="n">n_new</span><span class="p">,</span> <span class="mf">1.0</span><span class="p">)</span>
        <span class="n">mean_combined</span> <span class="o">=</span> <span class="p">(</span><span class="n">n_block</span> <span class="o">*</span> <span class="n">mean_block</span> <span class="o">+</span> <span class="n">n_total</span> <span class="o">*</span> <span class="n">mean_combined</span><span class="p">)</span> <span class="o">/</span> <span class="n">n_new_safe</span>
        <span class="n">M2_combined</span> <span class="o">=</span> <span class="n">M2_block</span> <span class="o">+</span> <span class="n">M2_combined</span> <span class="o">+</span> <span class="p">(</span><span class="n">delta</span> <span class="o">*</span> <span class="n">delta</span> <span class="o">*</span> <span class="n">n_total</span> <span class="o">*</span> <span class="n">n_block</span><span class="p">)</span> <span class="o">/</span> <span class="n">n_new_safe</span>
        <span class="n">n_total</span> <span class="o">=</span> <span class="n">n_new</span>

    <span class="n">var</span> <span class="o">=</span> <span class="n">M2_combined</span> <span class="o">/</span> <span class="n">N</span>

    <span class="c1"># Pass 2: normalize (must re-read tiles from HBM)
</span>    <span class="k">for</span> <span class="n">b</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_blocks</span><span class="p">):</span>
        <span class="n">offset</span> <span class="o">=</span> <span class="n">pid_m</span> <span class="o">*</span> <span class="n">stride_m</span> <span class="o">+</span> <span class="p">(</span><span class="n">b</span> <span class="o">*</span> <span class="n">BLOCK_N</span> <span class="o">+</span> <span class="n">offset_n</span><span class="p">)</span> <span class="o">*</span> <span class="n">stride_n</span>
        <span class="n">col_start</span> <span class="o">=</span> <span class="n">b</span> <span class="o">*</span> <span class="n">BLOCK_N</span>
        <span class="n">mask</span> <span class="o">=</span> <span class="n">col_start</span> <span class="o">+</span> <span class="n">offset_n</span> <span class="o">&lt;</span> <span class="n">N</span>
        <span class="n">w</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">weight_ptr</span> <span class="o">+</span> <span class="n">col_start</span> <span class="o">+</span> <span class="n">offset_n</span><span class="p">,</span> <span class="n">mask</span><span class="o">=</span><span class="n">mask</span><span class="p">,</span> <span class="n">other</span><span class="o">=</span><span class="mf">0.0</span><span class="p">)</span>
        <span class="n">bias</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">bias_ptr</span> <span class="o">+</span> <span class="n">col_start</span> <span class="o">+</span> <span class="n">offset_n</span><span class="p">,</span> <span class="n">mask</span><span class="o">=</span><span class="n">mask</span><span class="p">,</span> <span class="n">other</span><span class="o">=</span><span class="mf">0.0</span><span class="p">)</span>
        <span class="n">x_tile</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">x_ptr</span> <span class="o">+</span> <span class="n">offset</span><span class="p">,</span> <span class="n">mask</span><span class="o">=</span><span class="n">mask</span><span class="p">,</span> <span class="n">other</span><span class="o">=</span><span class="mf">0.0</span><span class="p">)</span>
        <span class="n">x_norm</span> <span class="o">=</span> <span class="p">(</span><span class="n">x_tile</span> <span class="o">-</span> <span class="n">mean_combined</span><span class="p">)</span> <span class="o">/</span> <span class="n">tl</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">var</span> <span class="o">+</span> <span class="n">eps</span><span class="p">)</span>
        <span class="n">out</span> <span class="o">=</span> <span class="n">x_norm</span> <span class="o">*</span> <span class="n">w</span> <span class="o">+</span> <span class="n">bias</span>
        <span class="n">tl</span><span class="p">.</span><span class="nf">store</span><span class="p">(</span><span class="n">output_ptr</span> <span class="o">+</span> <span class="n">offset</span><span class="p">,</span> <span class="n">out</span><span class="p">,</span> <span class="n">mask</span><span class="o">=</span><span class="n">mask</span><span class="p">)</span>
</code></pre></div></div> <p>The reloading of the tiles in Pass 2 is unavoidable — once the SRAM is full of the next tile, the previous tile’s data is gone. This is the HBM bandwidth cost of tiling, regardless of whether you use Welford or naive two-pass.</p> <hr/> <h2 id="references">References</h2> <ol> <li> <p><strong>PyTorch LayerNorm CUDA Source:</strong><br/> <a href="https://github.com/pytorch/pytorch/blob/main/aten/src/ATen/native/cuda/layer_norm_kernel.cu">github.com/pytorch/pytorch/…/layer_norm_kernel.cu</a></p> </li> <li><strong>Triton LayerNorm Tutorial:</strong><br/> <a href="https://github.com/triton-lang/triton/blob/main/python/tutorials/05-layer-norm.py">triton-lang.org/main/tutorials/05-layer-norm</a></li> <li><strong>Welford’s Original Paper (1962):</strong><br/> Welford, B. P. “Note on a Method for Calculating Corrected Sums of Squares and Products”</li> </ol> <hr/> <p><em>This post is part of my ongoing exploration of GPU kernel optimization and numerical stability in deep learning. Code examples and comparisons developed with AI assistance.</em></p>]]></content><author><name></name></author><category term="technical-deep-dive"/><category term="numerical-stability"/><category term="GPU-kernels"/><category term="PyTorch"/><category term="Triton"/><category term="LayerNorm"/><summary type="html"><![CDATA[When to use Welford's Algorithm?]]></summary></entry><entry><title type="html">NCCL Primitives Deep Dive</title><link href="https://ramchalamkr.github.io/blog/2026/nccl-primitives-deep-dive/" rel="alternate" type="text/html" title="NCCL Primitives Deep Dive"/><published>2026-02-15T05:01:00+00:00</published><updated>2026-02-15T05:01:00+00:00</updated><id>https://ramchalamkr.github.io/blog/2026/nccl-primitives-deep-dive</id><content type="html" xml:base="https://ramchalamkr.github.io/blog/2026/nccl-primitives-deep-dive/"><![CDATA[<p>When you’re training a 70B parameter LLM across 128 GPUs, every single GPU needs to coordinate with others. Whether it’s gathering weights, summing gradients, or routing tokens to experts in an MoE architecture, <strong>efficient communication is the bottleneck that determines if your training is rapid or not</strong>.</p> <p>In this post, we’ll dive deep into <strong>3 critical NCCL primitives</strong> that power modern distributed training:</p> <ol> <li><strong>All-Gather</strong> → Weight gathering across GPUs (tensor parallelism)</li> <li><strong>Ring All-Reduce</strong> → Gradient aggregation (data parallelism)</li> <li><strong>All-to-All</strong> → MoE token/output passing (expert parallelism)</li> </ol> <p>We’ll analyze the bandwidth requirements, walk through step-by-step examples, and show pseudo code to build intuition.</p> <hr/> <h2 id="why-these-primitives-matter">Why These Primitives Matter</h2> <p>Modern LLM training uses different parallelism strategies:</p> <ul> <li><strong>Tensor Parallelism</strong>: Shard model weights across GPUs → needs All-Gather</li> <li><strong>Data Parallelism</strong>: Each GPU has full model, processes different batches → needs All-Reduce for gradients</li> <li><strong>Expert Parallelism (MoE)</strong>: Experts on different GPUs, tokens routed dynamically → needs All-to-All</li> </ul> <p>Each strategy has different communication patterns, and using the wrong primitive is a recipe for disaster.</p> <hr/> <h2 id="1-all-gather-weight-gathering-across-gpus">1. All-Gather: Weight Gathering Across GPUs</h2> <h3 id="the-use-case">The Use Case</h3> <p>In <strong>tensor parallelism</strong> or <strong>FSDP Zero 3 variant</strong>, model weights are sharded across multiple GPUs. For example, a large linear layer might be split so each GPU holds <code class="language-plaintext highlighter-rouge">1/N</code> of the weight matrix. During the forward pass, each GPU needs the <strong>complete weight matrix</strong> to compute its output.</p> <p><strong>The Problem</strong>: How do we efficiently gather all shards on every GPU?</p> <h3 id="naive-approach-the-bottleneck">Naive Approach: The Bottleneck</h3> <p>The naive solution: Have each GPU send its shard to every other GPU.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code># Pseudo-code for naive all-gather
for each GPU i:
    for each other GPU j (j != i):
        send shard_i to GPU_j
</code></pre></div></div> <p><strong>Communication per GPU</strong>: Each GPU sends <code class="language-plaintext highlighter-rouge">M/N</code> data to <code class="language-plaintext highlighter-rouge">N-1</code> other GPUs = <code class="language-plaintext highlighter-rouge">M(N-1)/N</code> data sent.</p> <p><strong>The Bottleneck</strong>: What if all GPUs send simultaneously → network congestion! Also, each GPU must handle <code class="language-plaintext highlighter-rouge">N-1</code> separate send operations.</p> <h3 id="ring-all-gather-the-elegant-solution">Ring All-Gather: The Elegant Solution</h3> <p>The ring pattern in distributed systems is an architecture where nodes are organized in a logical or physical circle, with each node connecting to exactly two neighbors.</p> <p>In a <strong>ring topology</strong>, GPUs form a logical ring: <code class="language-plaintext highlighter-rouge">GPU_0 → GPU_1 → GPU_2 → ... → GPU_{N-1} → GPU_0</code></p> <p><strong>Algorithm</strong> (for <code class="language-plaintext highlighter-rouge">N</code> GPUs, each with shard of size <code class="language-plaintext highlighter-rouge">M/N</code>):</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Step 0: Each GPU has its own shard
Step 1: Pass shard to right neighbor (receive from left)
Step 2: Pass newly received shard to right neighbor
...
Step N-1: Complete after N-1 steps
</code></pre></div></div> <p>After <code class="language-plaintext highlighter-rouge">N-1</code> steps, every GPU has accumulated all N shards.</p> <p><strong>Communication Analysis</strong>:</p> <ul> <li>Each step: Send <code class="language-plaintext highlighter-rouge">M/N</code> data</li> <li>Total steps: <code class="language-plaintext highlighter-rouge">N-1</code></li> <li>Every GPU: Data sent and received per step is <code class="language-plaintext highlighter-rouge">M/N</code></li> <li><strong>Total data sent per GPU</strong>: <code class="language-plaintext highlighter-rouge">M(N-1)/N</code></li> <li><strong>Bandwidth utilization</strong>: Every link used simultaneously, no bottleneck!</li> </ul> <h3 id="step-by-step-example-4-gpus">Step-by-Step Example (4 GPUs)</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Initial state:
GPU_0: [A, _, _, _]
GPU_1: [_, B, _, _]
GPU_2: [_, _, C, _]
GPU_3: [_, _, _, D]

Step 1: GPU_i sends to GPU_{(i+1)%4}
GPU_0: [A, _, _, D]
GPU_1: [A, B, _, _]
GPU_2: [_, B, C, _]
GPU_3: [_, _, C, D]

Step 2: GPU_i sends the new shard again to GPU_{(i+1)%4}
GPU_0: [A, _, C, D]
GPU_1: [A, B, _, D]
GPU_2: [A, B, C, _]
GPU_3: [_, B, C, D]

Step 3: Repeat
GPU_0: [A, B, C, D]
GPU_1: [A, B, C, D]
GPU_2: [A, B, C, D]
GPU_3: [A, B, C, D]

</code></pre></div></div> <p>Compared to the naive approach, the total data sent per GPU remains the same <code class="language-plaintext highlighter-rouge">M(N-1)/N</code></p> <h3 id="pseudo-code">Pseudo Code</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="k">def</span> <span class="nf">ring_all_gather</span><span class="p">(</span><span class="n">shard</span><span class="p">,</span> <span class="n">rank</span><span class="p">,</span> <span class="n">num_gpu</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Ring All-Gather pseud code, for each GPU rank
    
    Args:
        shard: Local shard of size (M/N,) on this GPU
        rank: Current GPU rank (0 to world_size-1)
        num_gpu: Total number of GPUs (N)
    
    Returns:
        gathered: Complete data of size (M,) with all shards
    </span><span class="sh">"""</span>

    <span class="c1"># Initialize buffer to hold all shards (M/N)
</span>    <span class="n">shard_size</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">shard</span><span class="p">)</span> <span class="c1">#[4x1]
</span>    <span class="n">gathered</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">shard_size</span> <span class="o">*</span> <span class="n">num_gpu</span><span class="p">)</span>   <span class="c1">#[16x1]
</span>    
    <span class="c1"># Place local shard in correct position
</span>    <span class="n">gathered</span><span class="p">[</span><span class="n">rank</span> <span class="o">*</span> <span class="n">shard_size</span><span class="p">:(</span><span class="n">rank</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span> <span class="o">*</span> <span class="n">shard_size</span><span class="p">]</span> <span class="o">=</span> <span class="n">shard</span>
    
    <span class="c1"># Ring all-gather: N-1 steps
</span>    <span class="k">for</span> <span class="n">step</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_gpu</span> <span class="o">-</span> <span class="mi">1</span><span class="p">):</span>
        <span class="c1"># Which shard to send in this step?
</span>        <span class="n">send_idx</span> <span class="o">=</span> <span class="p">(</span><span class="n">rank</span> <span class="o">-</span> <span class="n">step</span><span class="p">)</span> <span class="o">%</span> <span class="n">num_gpu</span>
        
        <span class="c1"># Simulate send/receive with neighbors
</span>        <span class="n">send_shard</span> <span class="o">=</span> <span class="n">gathered</span><span class="p">[</span><span class="n">send_idx</span> <span class="o">*</span> <span class="n">shard_size</span> <span class="p">:</span> <span class="p">(</span><span class="n">send_idx</span><span class="o">+</span><span class="mi">1</span><span class="p">)</span> <span class="o">*</span> <span class="n">shard_size</span><span class="p">]</span>

        <span class="c1"># A conceptual function; Send to the right neighbour, receive from left neighbour
</span>        <span class="n">received_shard</span> <span class="o">=</span> <span class="nf">receive_from_left_neighbour</span><span class="p">(</span><span class="n">send_shard</span><span class="p">)</span>
        
        <span class="c1"># Place received shard
</span>        <span class="n">recv_idx</span> <span class="o">=</span> <span class="p">(</span><span class="n">rank</span> <span class="o">-</span> <span class="n">step</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span> <span class="o">%</span> <span class="n">num_gpu</span>
        <span class="n">gathered</span><span class="p">[</span><span class="n">recv_idx</span> <span class="o">*</span> <span class="n">shard_size</span><span class="p">:(</span><span class="n">recv_idx</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span> <span class="o">*</span> <span class="n">shard_size</span><span class="p">]</span> <span class="o">=</span> <span class="n">received_shard</span>
        
    <span class="k">return</span> <span class="n">gathered</span>

<span class="c1"># Example usage
</span><span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="c1"># Simulate 4 GPUs, each with a shard
</span>    <span class="n">num_gpu</span> <span class="o">=</span> <span class="mi">4</span>
    <span class="n">data_size</span> <span class="o">=</span> <span class="mi">16</span>
    <span class="n">shard_size</span> <span class="o">=</span> <span class="n">data_size</span> <span class="o">//</span> <span class="n">num_gpu</span>
    
    <span class="c1"># Each GPU has different shard
</span>    <span class="n">shards</span> <span class="o">=</span> <span class="p">[</span>
        <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">]),</span>  <span class="c1"># GPU 0 [4x1]
</span>        <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([</span><span class="mi">2</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">2</span><span class="p">]),</span>  <span class="c1"># GPU 1 [4x1]
</span>        <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([</span><span class="mi">3</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">3</span><span class="p">]),</span>  <span class="c1"># GPU 2 [4x1] 
</span>        <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([</span><span class="mi">4</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">4</span><span class="p">]),</span>  <span class="c1"># GPU 3 [4x1]
</span>    <span class="p">]</span>
    
    <span class="c1"># Each GPU runs all-gather, calls it async.
</span>    <span class="k">for</span> <span class="n">rank</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_gpu</span><span class="p">):</span>
        <span class="n">result</span> <span class="o">=</span> <span class="nf">ring_all_gather</span><span class="p">(</span><span class="n">shards</span><span class="p">[</span><span class="n">rank</span><span class="p">],</span> <span class="n">rank</span><span class="p">,</span> <span class="n">num_gpu</span><span class="p">)</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">GPU </span><span class="si">{</span><span class="n">rank</span><span class="si">}</span><span class="s"> result: </span><span class="si">{</span><span class="n">result</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <h3 id="why-ring-all-gather-outperforms-naive">Why Ring All-Gather Outperforms Naive</h3> <p>Both approaches send/receive the same total data: <code class="language-plaintext highlighter-rouge">M(N-1)/N</code> per GPU.</p> <p><strong>The difference is communication pattern:</strong></p> <ul> <li><strong>Naive</strong>: Each GPU → (N-1) GPUs simultaneously <ul> <li>Network interface contention</li> <li>May not align with physical topology</li> <li>Links underutilized or congested</li> </ul> </li> <li><strong>Ring</strong>: Each GPU → 1 neighbor at a time <ul> <li>Fully pipelined, all links active</li> <li>Maps to physical NVLink topology</li> <li>Optimal bandwidth utilization</li> </ul> </li> </ul> <hr/> <h2 id="2-ring-all-reduce-gradient-aggregation">2. Ring All-Reduce: Gradient Aggregation</h2> <h3 id="the-use-case-1">The Use Case</h3> <p>In <strong>data parallelism (or FSDP Zero1/2/3)</strong>, each GPU holds a complete copy of the model (or model shards) and processes a different batch of data. After the backward pass, each GPU computes gradients for all parameters. To update the model, we need the <strong>sum of all gradients</strong> on every GPU.</p> <p><strong>The Problem</strong>: How do we efficiently sum <code class="language-plaintext highlighter-rouge">N</code> gradient tensors of size <code class="language-plaintext highlighter-rouge">M</code> and distribute the result to all GPUs?</p> <h3 id="the-parameter-server-bottleneck">The Parameter Server Bottleneck</h3> <p>At first glance, it seems straightforward: just send everything to a central server, sum it up, and send it back. But if you try that with a 70B parameter model, your central server immediately becomes the bottleneck!</p> <p><strong>The Math of the Bottleneck</strong>:</p> <p>In a naive setting with a central parameter server:</p> <ul> <li>Server receives: <code class="language-plaintext highlighter-rouge">M × N</code> data (gradients from N GPUs)</li> <li>Server sends: <code class="language-plaintext highlighter-rouge">M × N</code> data (summed gradients to N GPUs)</li> </ul> <p>As you add more GPUs, the server’s bandwidth requirement <strong>scales linearly</strong>. The system chokes!</p> <p>This is where <strong>Ring All-Reduce</strong> comes into play.</p> <h3 id="ring-all-reduce-the-two-phase-solution">Ring All-Reduce: The Two-Phase Solution</h3> <p>Ring All-Reduce combines two primitives:</p> <p><strong>Phase 1: Reduce-Scatter</strong></p> <ul> <li>Break model gradients into <code class="language-plaintext highlighter-rouge">N</code> chunks</li> <li>After <code class="language-plaintext highlighter-rouge">N-1</code> steps, each GPU holds the <strong>complete sum</strong> for exactly <code class="language-plaintext highlighter-rouge">1/N</code> of the parameter gradients</li> </ul> <p><strong>Phase 2: All-Gather</strong></p> <ul> <li>Pass these completed shard sums around the ring</li> <li>After <code class="language-plaintext highlighter-rouge">N-1</code> steps, each GPU has the <strong>full summed gradient</strong></li> </ul> <p><strong>Total Communication</strong>: <code class="language-plaintext highlighter-rouge">2M(N-1)/N ≈ 2M</code> per GPU (almost independent of N!)</p> <h3 id="step-by-step-reduce-scatter">Step-by-Step Reduce-Scatter</h3> <p>Ring All reduce uses a reduce scatter primitive which does exactly as it sounds -&gt; Each GPU reduces its corresponding shard and sends to the neighbour. After <code class="language-plaintext highlighter-rouge">N-1</code> rounds, each GPU has the completed sum of its shard.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Initial state (4 GPUs, gradients split into 4 chunks):
GPU_0: [A0, B0, C0, D0]
GPU_1: [A1, B1, C1, D1]
GPU_2: [A2, B2, C2, D2]
GPU_3: [A3, B3, C3, D3]

Step 1: GPU_i sends its shard to GPU_{(i+1)%4}, and GPU_{(i+1)%4} accumulates the received shard.

GPU_0: [A0, B0, C0, D0+D3]
GPU_1: [A0+A1, B1, C1, D1]
GPU_2: [A2, B1+B2, C2, D2]
GPU_3: [A3, B3, C2+C3, D3]

Step 2: Repeats
GPU_0: [A0, B0, C0+C2+C3, D0+D3]
GPU_1: [A0+A1, B1, C1, D1+D0+D3]
GPU_2: [A2+A0+A1, B1+B2, C2, D2]
GPU_3: [A3, B3+B1+B2, C2+C3, D3]

Step 3: Repeats
GPU_0: [A0, B0+B3+B1+B2, C0+C2+C3, D0+D3]
GPU_1: [A0+A1, B1, C1+C0+C2+C3, D1+D0+D3]
GPU_2: [A2+A0+A1, B1+B2, C2, D2+D1+D0+D3]
GPU_3: [A3+A2+A0+A1, B3+B1+B2, C2+C3, D3]

Final: After reduce-scatter (Note: The other partials can be ignored / will be overwritten by the all-gather in the next step)
GPU_0: [_, B0+B1+B2+B3, _, _]
GPU_1: [_, _, C1+C0+C2+C3, _]
GPU_2: [_, _, _, D0+D1+D2+D3]
GPU_3: [A0+A1+A2+A3, _, _, _]

</code></pre></div></div> <h3 id="step-by-step-all-gather">Step-by-Step All-Gather</h3> <p>Refer to the previous section by using a ring all-gather approach.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>After all-gather, every GPU has:
[A0+A1+A2+A3, B0+B1+B2+B3, C0+C1+C2+C3, D0+D1+D2+D3]
</code></pre></div></div> <p><strong>Note</strong>: A keen eyed observer would realize, during the reduce scatter, the initial ownership of the shard has been shifted. For example,</p> <ul> <li><strong>Initial ownership</strong>: GPU 0 starts with A0, GPU 1 with B1, etc.</li> <li><strong>Final ownership (after reduce-scatter)</strong>: Chunks rotate, so GPU 0 ends up with sum(B), not sum(A)</li> </ul> <h3 id="pseudo-code-1">Pseudo Code</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="k">def</span> <span class="nf">reduce_scatter</span><span class="p">(</span><span class="n">chunks</span><span class="p">,</span> <span class="n">rank</span><span class="p">,</span> <span class="n">num_gpu</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Reduce-Scatter: Each GPU gets sum of one chunk
    
    Args:
        chunks: List of N chunks, each of size M/N
        rank: Current GPU rank
        num_gpu: Total number of GPUs
    
    Returns:
        buffer: returns the full reduced buffer
    </span><span class="sh">"""</span>
    <span class="n">chunk_size</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">chunks</span><span class="p">[</span><span class="mi">0</span><span class="p">])</span> <span class="c1">#[4]
</span>    <span class="nb">buffer</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">concatenate</span><span class="p">(</span><span class="n">chunks</span><span class="p">)</span> <span class="c1">#[16x1]
</span>
    <span class="k">for</span> <span class="n">step</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_gpu</span><span class="o">-</span><span class="mi">1</span><span class="p">):</span>
        <span class="n">step_idx</span> <span class="o">=</span> <span class="p">(</span><span class="n">rank</span> <span class="o">-</span> <span class="n">step</span><span class="p">)</span> <span class="o">%</span> <span class="n">num_gpu</span>
        <span class="n">send_shard</span> <span class="o">=</span> <span class="nb">buffer</span><span class="p">[</span><span class="n">step_idx</span><span class="o">*</span><span class="n">chunk_size</span><span class="p">:</span> <span class="p">(</span><span class="n">step_idx</span><span class="o">+</span><span class="mi">1</span><span class="p">)</span><span class="o">*</span><span class="n">chunk_size</span><span class="p">]</span>

        <span class="c1"># Simulated function to reduce scatter: Send to right, Receive from left.
</span>        <span class="n">received_shard_sum</span> <span class="o">=</span> <span class="nf">reduce_scatter_from_left</span><span class="p">(</span><span class="n">send_shard</span><span class="p">)</span>

        <span class="n">received_idx</span> <span class="o">=</span> <span class="p">(</span><span class="n">rank</span> <span class="o">-</span> <span class="n">step</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span> <span class="o">%</span> <span class="n">num_gpu</span>
        <span class="nb">buffer</span><span class="p">[</span><span class="n">received_idx</span><span class="o">*</span><span class="n">chunk_size</span> <span class="p">:</span> <span class="p">(</span><span class="n">received_idx</span><span class="o">+</span><span class="mi">1</span><span class="p">)</span><span class="o">*</span><span class="n">chunk_size</span><span class="p">]</span> <span class="o">+=</span> <span class="n">received_shard_sum</span>

    <span class="k">return</span> <span class="nb">buffer</span>   <span class="c1">#[16x1]
</span>
<span class="k">def</span> <span class="nf">ring_all_reduce</span><span class="p">(</span><span class="n">gradient</span><span class="p">,</span> <span class="n">rank</span><span class="p">,</span> <span class="n">num_gpu</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Ring All-Reduce: Sum gradients across all GPUs
    
    Args:
        gradient: Local gradient tensor of size M
        rank: Current GPU rank
        num_gpu: Total number of GPUs
    
    Returns:
        summed_gradient: Sum of all gradients, size M
    </span><span class="sh">"""</span>
    
    
    <span class="c1"># Step 1: Split gradient into chunks
</span>    <span class="n">chunk_size</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">gradient</span><span class="p">)</span> <span class="o">//</span> <span class="n">num_gpu</span>   <span class="c1">#[16/4 = 4]
</span>    <span class="n">chunks</span> <span class="o">=</span> <span class="p">[</span><span class="n">gradient</span><span class="p">[</span><span class="n">i</span><span class="o">*</span><span class="n">chunk_size</span><span class="p">:(</span><span class="n">i</span><span class="o">+</span><span class="mi">1</span><span class="p">)</span><span class="o">*</span><span class="n">chunk_size</span><span class="p">]</span> <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_gpu</span><span class="p">)]</span>
    
    <span class="c1"># Step 2: Reduce-scatter phase
</span>    <span class="n">reduced_chunks</span> <span class="o">=</span> <span class="nf">reduce_scatter</span><span class="p">(</span><span class="n">chunks</span><span class="p">,</span> <span class="n">rank</span><span class="p">,</span> <span class="n">num_gpu</span><span class="p">)</span>
    
    <span class="c1"># Step 3: All-gather phase
</span>    <span class="n">summed_gradient</span> <span class="o">=</span> <span class="nf">ring_all_gather</span><span class="p">(</span><span class="n">reduced_chunks</span><span class="p">,</span> <span class="n">rank</span><span class="p">,</span> <span class="n">num_gpu</span><span class="p">)</span>
    
    <span class="k">return</span> <span class="n">summed_gradient</span>

<span class="c1"># Example usage
</span><span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="c1"># Simulate 4 GPUs, each with gradients
</span>    <span class="n">num_gpu</span> <span class="o">=</span> <span class="mi">4</span>
    <span class="n">gradient_size</span> <span class="o">=</span> <span class="mi">16</span>
    
    <span class="n">gradients</span> <span class="o">=</span> <span class="p">[</span>
        <span class="n">np</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="n">gradient_size</span><span class="p">)</span> <span class="o">*</span> <span class="mi">1</span><span class="p">,</span>  <span class="c1"># GPU 0    [16x1]
</span>        <span class="n">np</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="n">gradient_size</span><span class="p">)</span> <span class="o">*</span> <span class="mi">2</span><span class="p">,</span>  <span class="c1"># GPU 1    [16x1]
</span>        <span class="n">np</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="n">gradient_size</span><span class="p">)</span> <span class="o">*</span> <span class="mi">3</span><span class="p">,</span>  <span class="c1"># GPU 2    [16x1]
</span>        <span class="n">np</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="n">gradient_size</span><span class="p">)</span> <span class="o">*</span> <span class="mi">4</span><span class="p">,</span>  <span class="c1"># GPU 3    [16x1]
</span>    <span class="p">]</span>
    
    <span class="n">expected_sum</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="n">gradient_size</span><span class="p">)</span> <span class="o">*</span> <span class="mi">10</span>  <span class="c1"># Sum = 1+2+3+4
</span>    
    <span class="k">for</span> <span class="n">rank</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_gpu</span><span class="p">):</span>
        <span class="n">result</span> <span class="o">=</span> <span class="nf">ring_all_reduce</span><span class="p">(</span><span class="n">gradients</span><span class="p">[</span><span class="n">rank</span><span class="p">],</span> <span class="n">rank</span><span class="p">,</span> <span class="n">num_gpu</span><span class="p">)</span>
        <span class="c1"># print(f"GPU {rank} summed gradient: {result}")
</span></code></pre></div></div> <hr/> <h2 id="3-all-to-all-moe-token-routing">3. All-to-All: MoE Token Routing</h2> <h3 id="the-use-case-2">The Use Case</h3> <p>In <strong>Mixture of Experts (MoE)</strong> architectures, you have <code class="language-plaintext highlighter-rouge">E</code> experts distributed across <code class="language-plaintext highlighter-rouge">N</code> GPUs. During the forward pass:</p> <ol> <li>A routing network decides which expert should process each token</li> <li>Tokens need to be <strong>grouped</strong> to the GPU hosting their assigned expert</li> <li>After expert computation, outputs need to be <strong>grouped back</strong></li> </ol> <p><strong>The Problem</strong>: Each GPU needs to send different amounts of data to each other GPU (dynamic routing). This is a <strong>gpu specific exchange</strong>.</p> <h3 id="why-all-gather-wont-work">Why All-Gather Won’t Work</h3> <p>All-Gather assumes every GPU contributes the same amount of data. In MoE, routing is <strong>dynamic</strong>:</p> <ul> <li>GPU 0 might send 10 tokens to GPU 1, 5 to GPU 2, 0 to GPU 3</li> <li>GPU 1 might send 3 tokens to GPU 0, 8 to GPU 2, 4 to GPU 3</li> </ul> <p>We need a primitive that supports <strong>arbitrary dynamic exchange</strong>.</p> <h3 id="all-to-all-personalized-communication">All-to-All: Personalized Communication</h3> <p>In an <strong>All-to-All</strong> primitive:</p> <ul> <li>Each GPU has a separate information for every other GPU</li> <li>All GPUs simultaneously exchange their specific messages</li> </ul> <p><strong>Algorithm</strong>:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>for each GPU i:
    for each GPU j:
        send message_i_to_j to GPU_j
        receive message_j_to_i from GPU_j
</code></pre></div></div> <p><strong>Communication</strong>: In the worst case, if each GPU sends <code class="language-plaintext highlighter-rouge">M/N</code> tokens to each other GPU, total sent = <code class="language-plaintext highlighter-rouge">M(N-1)/N</code> per GPU.</p> <p><strong>MoE Specifics</strong>:</p> <ul> <li>Forward pass: All-to-All to route tokens to experts</li> <li>Backward pass: All-to-All to route gradients back to original GPUs</li> </ul> <p><strong>Basic View</strong> All-to-All exchanges data between all GPUs in a group.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Think of data as a matrix where rows = source GPU, columns = destination GPU

Before All-to-All:
       Dest: GPU0  GPU1  GPU2  GPU3
Source GPU0:  A0    A1    A2    A3
       GPU1:  B0    B1    B2    B3
       GPU2:  C0    C1    C2    C3
       GPU3:  D0    D1    D2    D3

After All-to-All (transpose):
       Dest: GPU0  GPU1  GPU2  GPU3
Source GPU0:  A0    B0    C0    D0
       GPU1:  A1    B1    C1    D1
       GPU2:  A2    B2    C2    D2
       GPU3:  A3    B3    C3    D3

</code></pre></div></div> <h3 id="step-by-step-example-4-gpus-8-experts">Step-by-Step Example (4 GPUs, 8 Experts)</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Scenario: 16 tokens, 4 GPUs, 8 experts (2 experts per GPU) 
Data Parallelism + Expert Parallelism

GPU 0: Experts [E0, E1]
GPU 1: Experts [E2, E3]
GPU 2: Experts [E4, E5]
GPU 3: Experts [E6, E7]

Tokens on GPU 0: [T0, T1, T2, T3]
Routing decisions:
  T0 → E2 (GPU 1)
  T1 → E5 (GPU 2)
  T2 → E0 (GPU 0, stays local)
  T3 → E7 (GPU 3)

All-to-All exchange:
  GPU 0 sends: [T0] to GPU 1, [T1] to GPU 2, [T3] to GPU 3
  GPU 0 receives: tokens routed to E0 and E1 from other GPUs


</code></pre></div></div> <p><strong><em>All-to-All</em></strong> redistributes tokens from different sequences to the GPUs hosting their assigned experts. This makes sense because:</p> <ul> <li>GPU 0 has sequence 0’s tokens but needs to send them to other experts</li> <li>GPU 0 receives tokens from OTHER sequences that need its local experts E0, E1</li> </ul> <h2 id="expert-parallelism-flow">Expert Parallelism Flow</h2> <p><strong>Forward Pass</strong>:</p> <p><strong>Step 1: Routing</strong> (local on each GPU)</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Each GPU routes its tokens
</span><span class="n">router_logits</span> <span class="o">=</span> <span class="nf">router</span><span class="p">(</span><span class="n">tokens</span><span class="p">)</span>  <span class="c1"># Local
</span><span class="n">top_k_experts</span> <span class="o">=</span> <span class="nf">topk</span><span class="p">(</span><span class="n">router_logits</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="mi">2</span><span class="p">)</span>
</code></pre></div></div> <p><strong>Step 2: Dispatch</strong> (All-to-All)</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Group tokens by destination expert
# All-to-All sends tokens to expert's GPU
</span><span class="n">dispatched_tokens</span> <span class="o">=</span> <span class="nf">all_to_all</span><span class="p">(</span><span class="n">grouped_tokens</span><span class="p">)</span>
</code></pre></div></div> <p><strong>Step 3: Expert Computation</strong> (local)</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Each GPU processes its experts' tokens
</span><span class="k">for</span> <span class="n">expert</span> <span class="ow">in</span> <span class="n">my_experts</span><span class="p">:</span>
    <span class="n">expert_output</span> <span class="o">=</span> <span class="nf">expert</span><span class="p">(</span><span class="n">dispatched_tokens</span><span class="p">[</span><span class="n">expert_id</span><span class="p">])</span>
</code></pre></div></div> <p><strong>Step 4: Combine</strong> (All-to-All)</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Send results back to originating GPUs
</span><span class="n">combined_output</span> <span class="o">=</span> <span class="nf">all_to_all</span><span class="p">(</span><span class="n">expert_outputs</span><span class="p">)</span>
</code></pre></div></div> <p><strong>Communication cost</strong>: 2 All-to-Alls per MoE layer</p> <hr/> <h3 id="simplified-pseudo-code">Simplified Pseudo Code</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">all_to_all_moe</span><span class="p">(</span><span class="n">tokens</span><span class="p">,</span> <span class="n">routing</span><span class="p">,</span> <span class="n">rank</span><span class="p">,</span> <span class="n">num_gpus</span><span class="p">,</span> <span class="n">num_experts</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    All-to-All for MoE token routing
    
    Args:
        tokens: List of token IDs on this GPU
        routing: Expert assignment for each token
        rank: Current GPU rank
        num_gpus: Total number of GPUs
        num_experts: Total number of experts
    
    Returns:
        Tokens this GPU will process after All-to-All
    </span><span class="sh">"""</span>
    <span class="n">experts_per_gpu</span> <span class="o">=</span> <span class="n">num_experts</span> <span class="o">//</span> <span class="n">num_gpus</span>
    
    <span class="c1"># Group tokens by destination GPU based on expert assignment
</span>    <span class="n">send_to_gpu</span> <span class="o">=</span> <span class="p">{</span><span class="n">i</span><span class="p">:</span> <span class="p">[]</span> <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_gpus</span><span class="p">)}</span>
    
    <span class="k">for</span> <span class="n">token_id</span><span class="p">,</span> <span class="n">expert_id</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(</span><span class="n">tokens</span><span class="p">,</span> <span class="n">routing</span><span class="p">):</span>
        <span class="n">target_gpu</span> <span class="o">=</span> <span class="n">expert_id</span> <span class="o">//</span> <span class="n">experts_per_gpu</span>
        <span class="n">send_to_gpu</span><span class="p">[</span><span class="n">target_gpu</span><span class="p">].</span><span class="nf">append</span><span class="p">((</span><span class="n">token_id</span><span class="p">,</span> <span class="n">expert_id</span><span class="p">))</span>
    
    <span class="c1"># All to ALL will happen here to send and receive the tokens
</span>    
    <span class="c1"># For this case, return what this GPU keeps locally + tokens received from all to all for this gpu
</span>    <span class="k">return</span> <span class="n">send_to_gpu</span><span class="p">[</span><span class="n">rank</span><span class="p">]</span>

<span class="c1"># Example: GPU 0 in MoE with Data Parallelism
</span><span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="n">num_gpus</span> <span class="o">=</span> <span class="mi">4</span>
    <span class="n">num_experts</span> <span class="o">=</span> <span class="mi">8</span>
    
    <span class="c1"># GPU 0 has tokens from Sequence 0
</span>    <span class="n">rank</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">tokens</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">]</span>  <span class="c1"># Token IDs: T0, T1, T2, T3
</span>    <span class="n">routing</span> <span class="o">=</span> <span class="p">[</span><span class="mi">2</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">7</span><span class="p">]</span>  <span class="c1"># T0→E2, T1→E5, T2→E0, T3→E7
</span>    
    <span class="c1"># Expert distribution across GPUs:
</span>    <span class="c1"># GPU 0: E0, E1
</span>    <span class="c1"># GPU 1: E2, E3
</span>    <span class="c1"># GPU 2: E4, E5
</span>    <span class="c1"># GPU 3: E6, E7
</span>    
    <span class="n">local_tokens</span> <span class="o">=</span> <span class="nf">all_to_all_moe</span><span class="p">(</span><span class="n">tokens</span><span class="p">,</span> <span class="n">routing</span><span class="p">,</span> <span class="n">rank</span><span class="p">,</span> <span class="n">num_gpus</span><span class="p">,</span> <span class="n">num_experts</span><span class="p">)</span>
    <span class="c1"># GPU 0 keeps: Token 2 for Expert 0
</span>    <span class="c1"># GPU 0 receives: Tokens from other GPUs routed to E0, E1
</span></code></pre></div></div> <hr/> <h2 id="bandwidth-analysis--comparison">Bandwidth Analysis &amp; Comparison</h2> <table> <thead> <tr> <th>Primitive</th> <th>Use Case</th> <th>Data Sent Per GPU</th> <th>Bandwidth Scaling</th> </tr> </thead> <tbody> <tr> <td><strong>All-Gather</strong></td> <td>Weight gathering (TP)</td> <td><code class="language-plaintext highlighter-rouge">M(N-1)/N</code></td> <td>(<code class="language-plaintext highlighter-rouge">~M</code>)</td> </tr> <tr> <td><strong>All-Reduce</strong></td> <td>Gradient aggregation (DP)</td> <td><code class="language-plaintext highlighter-rouge">2M(N-1)/N</code></td> <td>(<code class="language-plaintext highlighter-rouge">~2M</code>)</td> </tr> <tr> <td><strong>All-to-All</strong></td> <td>Token routing (MoE)</td> <td>Variable, up to <code class="language-plaintext highlighter-rouge">M(N-1)/N</code></td> <td>Depends on routing</td> </tr> </tbody> </table> <p><strong>Key Insight</strong>: All three primitives scale <strong>near-constant</strong> with the number of GPUs!</p> <h3 id="all-gather-as-a-special-case-of-all-to-all">All-Gather as a Special Case of All-to-All</h3> <p><strong>All-Gather is actually a special case of All-to-All</strong> where each GPU sends the <strong>same data</strong> to everyone.</p> <p><strong>Example with 4 GPUs:</strong></p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Initial state:
GPU 0: [1]
GPU 1: [2]
GPU 2: [3]
GPU 3: [4]

All-Gather behavior:
GPU 0 sends [1] to GPU 1, 2, 3
GPU 1 sends [2] to GPU 0, 2, 3
GPU 2 sends [3] to GPU 0, 1, 3
GPU 3 sends [4] to GPU 0, 1, 2

Result (all GPUs):
[1, 2, 3, 4]
</code></pre></div></div> <p><strong>Same result using All-to-All:</strong></p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GPU 0 All-to-All send:
  To GPU 0: [1]  (keep local)
  To GPU 1: [1]  (same shard)
  To GPU 2: [1]  (same shard)
  To GPU 3: [1]  (same shard)

GPU 1 All-to-All send:
  To GPU 0: [2]
  To GPU 1: [2]  (keep local)
  To GPU 2: [2]
  To GPU 3: [2]

... (GPU 2, 3 similarly)

Result (all GPUs):
[1, 2, 3, 4]  ✓ Same as All-Gather!
</code></pre></div></div> <p><strong>The Difference:</strong></p> <ul> <li><strong>All-Gather</strong>: Everyone sends their shard to everyone (broadcast pattern)</li> <li><strong>All-to-All (MoE)</strong>: Each GPU sends <strong>different, selective data</strong> based on routing <ul> <li>GPU 0 might send [T0] to GPU 1, [T1] to GPU 2, [T2] to GPU 3</li> <li>More efficient for sparse, dynamic patterns!</li> </ul> </li> </ul> <h2 id="conclusion">Conclusion</h2> <p><strong>The Big Picture</strong>:</p> <ul> <li><strong>All-Gather</strong>: Collect sharded data without reduction</li> <li><strong>All-Reduce</strong>: Aggregate (sum) and distribute</li> <li><strong>All-to-All</strong>: Personalized, dynamic exchange</li> </ul> <p>All three use clever topologies (rings) to avoid bottlenecks and utilize full network bandwidth.</p> <hr/> <h2 id="references">References</h2> <ol> <li> <p>HuggingFace Ultra-Scale Playbook<br/> <a href="https://huggingface.co/spaces/weege007/ultrascale-playbook">https://huggingface.co/spaces/weege007/ultrascale-playbook</a></p> </li> <li> <p>Machine Learning Systems for Dummies - Abhishek Maiti<br/> <a href="https://abhishekmaiti.com/mlsys-for-dummies/">https://abhishekmaiti.com/mlsys-for-dummies/</a></p> </li> <li> <p>NCCL Overview - NVIDIA<br/> <a href="https://docs.nvidia.com/deeplearning/nccl/user-guide/docs/overview.html">https://docs.nvidia.com/deeplearning/nccl/user-guide/docs/overview.html</a></p> </li> </ol> <hr/> <p><em>This post is part of my ongoing deep dive into systems-level ML engineering on GPUs. Some diagrams and examples were developed with AI assistance.</em></p>]]></content><author><name></name></author><category term="technical-deep-dive"/><category term="distributed-training"/><category term="NCCL"/><category term="LLM"/><category term="MoE"/><category term="communication"/><summary type="html"><![CDATA[Understanding All-Gather, Ring All-Reduce, and All-to-All communication primitives in LLMs]]></summary></entry></feed>